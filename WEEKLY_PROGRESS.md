# ReTrace-ASR 本周工作进展

> 周期：2026-08-10 ～ 2026-08-13　|　作者：ReTrace-ASR 项目组

---

## 一、项目介绍

### 1.1 研究问题

流式对话 ASR 的致命痛点是**无法回头**：一旦某个专有名词（人名、游戏角色名、品牌、术语）被听错，后续所有依赖它的语义判断都会连锁出错。**ReTrace-ASR** 要回答的研究问题是：**当 ASR 在流式对话中产生识别错误时，系统能否像人类一样"存疑 → 重听 → 修正"，而不是无条件信任单次识别结果？**

### 1.2 核心思想

系统的核心思想是"时序记忆 + 回溯修正"，四个设计原则贯穿始终：

- **不信任单遍识别**：首遍 ASR 结果作为不可修改的原始观察（`raw_text`）冻结持久化，`current_text` 只由 raw 与只追加的账本回放得到，原始证据永不丢失。
- **自主存疑**：Context Judge（DeepSeek 大模型）结合短期与长期记忆判断新识别内容与已知上下文的关系，主动标记"存疑/冲突"，而不是被动等错误累积。
- **定向重听**：只针对 Agent 指定的历史音频窗口与候选词做闭集验证，不做全局 N-gram 扫描——把语义发现、声学授权、并发一致性三者分离。
- **可审计回溯**：每次修订/回滚都是只追加的事件，携带目标 turn、证据 turn、音频区间、候选分数与理由，全程无需人工确认。

### 1.3 三个核心创新假设（面向投稿）

1. **对称的时间化 ASR 信念仲裁**：不预设"新文本"或"旧记忆"谁更可信，而是把新旧解释建模为可竞争、可共存、可随时间变化的信念，后续证据既能修订历史 turn，也能自动回滚先前修订。
2. **与字幕因果依赖耦合的双时标记忆**：短期记忆保存工作信念、竞争假设和 turn 依赖；长期记忆只提升具有独立来源或声学验证的版本化信念，且不直接批量改写历史字幕——把"记忆帮助"与"记忆污染"同时变成可测量对象。
3. **Agent 触发的选择性历史声学验证**：judge 在任何全局候选扫描之前决定是否存在冲突，只对目标 span 请求闭集音频验证，用开发集标定概率与 margin 门控，在高修订召回目标下限制过度修订。

### 1.4 技术栈与代码规模

| 层次 | 技术选型 | 规模 |
| --- | --- | --- |
| 后端 | FastAPI + uvicorn（Python 3.10） | 约 3800 行 |
| ASR | Qwen-Omni（vLLM，双卡 GPU 0/1 并行） | `qwen_asr.py` 720 行 |
| 语义判断 | DeepSeek（结构化 Context Judge） | `deepseek.py` 172 行 |
| 编排核心 | `retrace.py`（五步流水线） | 756 行 |
| 前端 | Vite + TypeScript + SSE | `main.ts / api.ts / styles.css` |
| 测试 | pytest，注入式 judge/验证器 | 18 个文件、111 个测试 |

---

## 二、本周工作概述

本周完成了从"可运行原型"到"防误报能力收敛"的完整闭环，四条主线：

1. **双卡并行推理服务**：搭建 GPU 0/1 双卡 Qwen-Omni 并行推理，打通文本 + 音频双入口流水线，20 分钟音频实测约 15 分钟处理完。
2. **流式实时转写**：实现 SSE 逐 turn 推送，上传音频立即返回，前端三面板实时渲染。
3. **回溯修订核心机制**：开放重听兜底、记忆热词引导、judge 格式容错等关键能力补齐。
4. **三轮防误报迭代**（本周最大投入）：系统从"会瞎改"收敛到"宁可不改、也不改错"。

---

## 三、核心工作详述

### 3.1 双卡并行推理服务

- 服务运行于 `http://127.0.0.1:8000`，`ASR_GPU=0,1` 双卡各 spawn 一个 Qwen-Omni worker，chunk 按 round-robin 分片并行推理。
- `preload_engine()` 启动时加载双卡模型（约 1-2 分钟），`asr_status()` 实时报告 `loaded / workers / gpus`。
- 长音频切分为 chunk（`split_audio_file`，带 overlap），推理后 `join_transcripts` 拼接，流式路径用 `_OrderedChunkProcessor` 缓冲乱序 chunk 严格按 index 有序分析。

### 3.2 五步分析流水线

```
OBSERVE → MEMORY RETRIEVE → CONTEXT JUDGE → TARGETED RELISTEN → EVENT REPLAY
```

- **OBSERVE**：立即冻结并持久化 `raw_text` 与时间戳，文本接口返回 `202 queued`。
- **MEMORY RETRIEVE**：读取近期 turn、未决假设、依赖 turn 和长期稳定事实。
- **CONTEXT JUDGE**：Agent 输出 `CONSISTENT / NOVEL / CONFLICT / UNCERTAIN`，冲突进一步区分互斥、共存、现实变化三类，并给出带 turn 证据的结构化信念。
- **TARGETED RELISTEN**：只对 Agent 指定的历史音频窗口和候选词做闭集验证。
- **EVENT REPLAY**：用不可变 raw turn + 只追加账本重建当前字幕，七种动作（`ACCEPT_NEW / KEEP_OLD / REVISE_CURRENT / REVISE_HISTORY / COEXIST / DEFER / ROLLBACK`）全可审计。

### 3.3 双层记忆机制

- **短期记忆**：近期上下文、工作事实（WorkingBeliefs）、未决假设（OpenHypotheses）与依赖索引（DependencyIndex）。
- **长期记忆**：只接收达到置信门槛且具备独立来源或音频验证的稳定事实，带置信度、时间有效性、来源会话、版本与 supersedes 链。
- **每 session 隔离**：长期记忆落盘 `.memory/{session_id}.json`，多会话互不污染。
- 本周修复长期记忆长期为空的问题：阈值 0.85 → 0.70，独立支持判定放宽为"跨 session ≥ 2 或同 session 跨 turn ≥ 2"。

### 3.4 流式实时转写

- 上传音频后立即返回，后台 `_stream_audio_session_background` 按 chunk 异步分析，通过 SSE `GET /api/sessions/{id}/events` 逐 turn 推送 `session / turn / asr_done / done / error` 事件。
- SSE key 用 `bound_session`（从音频路径推导），不能用请求里的 demo/auto id。
- 前端 `submitAudio` 用 EventSource 逐 turn 渲染，支持三面板折叠、内容区内部滚动、只保留音频输入的紧凑布局。

### 3.5 回溯修订核心机制

- **开放重听兜底**（`_relisten_uncertain_window`）：judge 判定 UNCERTAIN/CONFLICT 但无法指明具体错误词时，回到音频窗口开放式重听，覆盖"记忆里没有正确词"的场景。
- **记忆热词引导**：重听时把记忆中的领域实体作为热词传给 Qwen，引导其优先识别为正确专有名词。
- **judge 格式容错**（`context_judge.py::_as_list`）：DeepSeek 返回 `"focus": {...}` 单对象而非数组时包装为单元素列表，此前该问题导致大量 turn 报 `expected list or null, got dict` 而整体 judge 失效。
- **judgment 置信度兜底**：`_fill_judgment_confidence()` 为缺失 confidence 的 judge 输出按 outcome 兜底（CONSISTENT 0.8 / NOVEL 0.75 / CONFLICT 0.85 / UNCERTAIN 0.5），避免前端一律显示 0%。
- **退化文本恢复**：`assess_transcript` 检测重复填充词等退化输出，`trim_degenerate_tail` 从首次重复处裁剪保留有效前缀，重听仍退化时兜底裁剪。

### 3.6 防误报迭代（三轮，本周重点攻坚）

系统最初具备较强的"主动存疑"能力，但在真实音频上暴露出**误报（瞎改）**问题。经过三轮迭代，系统从"会瞎改"收敛到"宁可不改、也不改错"。

**第一轮：确定性音近检测误报**

- 现象：用 pypinyin 做确定性音近检测（difflib ratio 阈值 0.62），把正常词"时候"误判为英雄名"狮子狗"（拼音相似度 0.714），把"狮子狗刚出的时候"改成"狮子狗刚出的狮子狗"。
- 修复：① 实体已在本 turn 正确出现则不再参与比对；② 停用词从"完全相等"改为"包含子串即跳过"，拦截滑动窗口切出的"的时候"等功能词组合。

**第二轮：重听被过时热词带偏**

- 现象：重听时把记忆里过时的实体 `CS:GO` 当热词，导致英雄联盟音频被重听成"C S go连加二"，反而比原文更差。
- 修复：① 重听结果"不同"≠"更好"——只有命中记忆正确实体或恢复退化文本时才采信；② resolver 拒绝"把两个都已出现在原文里的词互相替换"的低置信名称互换（验证器听整窗音频，会被窗口内其他位置的词误导）。

**第三轮：移除确定性音近检测**

- 结论：音近检测的收益场景与 LLM 语义判断完全重叠（LLM 能做得更好），误报面却无限宽（中文普通词音近极多），而真正难的问题（正确实体从未进入记忆）它又帮不上。
- 决策：彻底移除确定性音近检测的抢先裁决路径，将音近纠正职责完全交还 DeepSeek 的世界知识与语义判断（配合领域词表 `domain_entities` + 精心设计的提示词）。同时清理提示词中硬编码的具体例子，改为抽象的原则描述。

---

## 四、测试与验证

- **18 个测试文件、111 个自动化测试全部通过**（含本轮新增/删除后的最终状态）。
- 覆盖范围：记忆机制、退化检测、上下文判定、证据验证、账本回滚、流式协调、双卡分片、API 接口、前端快照。
- 测试采用注入式 judge / 音频验证器与合成会话，可离线运行，不依赖真实模型与数据集。
- 协议合规边界明确：当前不宣称 CER/WER、显著性等真实实验结论，门槛为显式 bootstrap 配置，待开发集标定。

---

## 五、下周计划

1. **接入真实开发集标定**：用 RAMC / Alimeeting 开发集重新标定 `calibration.py` 中的 `suspect / relisten / revise / long_memory / audio_margin` 门槛。
2. **评测指标报告**：报告 revision recall、F2/F3、overcorrection、rollback 成功率与 Memory 污染率。
3. **消融实验设计**：分别去掉未来证据、短期记忆、长期记忆、声学验证、回滚、版本重验证，在相同过度修订率下比较历史修订召回率与最终 CER/WER。
4. **继续打磨防误报**：在真实长音频上持续观察"该改的没改 / 不该改的瞎改"两类问题，巩固保守修订策略。
5. **补全成本测量**：每会话复听秒数、DeepSeek 调用次数、端到端延迟曲线。

---

## 六、关键经验沉淀

- **"不同"不等于"更好"**：重听/二次识别是同一个 ASR 模型，可能被过时热词带偏，采信前必须有独立证据。
- **确定性启发式易误报**：拼音相似度等弱信号跳过语义判断直接出结论，误报面极宽，最终让位于 LLM 的语义级判断。
- **证据链优先**：无历史音频或验证失败时固定 DEFER，绝不只凭文本改写字幕。
- **验证器的位置错位**：闭集验证听的是整窗音频，若候选词在窗口其他位置出现会误导打分，需在 resolver 层加防线。
